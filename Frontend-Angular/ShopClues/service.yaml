apiVersion: v1
kind: Service
metadata:
  name: shopclues-app-service
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-resource-group: my-azure-ecomm
spec:
  type: LoadBalancer
  selector:
    app: shopclues-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
