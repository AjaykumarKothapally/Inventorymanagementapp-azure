apiVersion: v1
kind: Service
metadata:
  name: backend-shopwise-app-service
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-resource-group: my-ecomm-azure
spec:
  type: LoadBalancer
  selector:
    app: shopwise
  ports:
    - protocol: TCP
      port: 9091
      targetPort: 9091
